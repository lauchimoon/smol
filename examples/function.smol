fn factorial(x: int) int {
    let f: int = 1;
    let it: int = 1;
    while (it <= x) {
        f = f*it;
        it = it + 1;
    }
    return f;
}

fn main() void {
    let x: int = 6;
    print "6! = ";
    println factorial(x);
}
