fn main() {
    let x0: int = 18;
    let y0: int = 6;

    let x: int = x0;
    let y: int = y0;
    while (x != y) {
        if (x > y) {
            x = x - y;
        } else {
            y = y - x;
        }
    }
    printf("gcd(%d, %d) = %d\n", x0, y0, x);
}
